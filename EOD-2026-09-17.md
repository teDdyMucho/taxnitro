EOD: TaxNitro – a chat assistant in the portal, and the procedures it answers from were read out of the code

1. Added a floating chat button to the client portal, bottom right on every tab. It opens a panel, sends what the client types to the n8n chatbot flow, and shows the reply.
2. No chatbot logic lives in the app. It sends a message and renders the answer — the SOPs, the Slack ticket and the escalation are all n8n's.
3. Built the knowledge base it answers from: a `sops` table plus 21 written procedures, 10 for clients and 11 for staff.
4. Every procedure was read out of the code rather than written from memory, so each says what the software actually does today.
5. The client bot cannot read the internal procedures. Tested it: a client session searching for internal topics gets nothing back, and that holds even if the bot is asked to ignore its instructions, because the database refuses rather than the prompt.
6. Writing them turned up ten places where the software does not do what its buttons say. Listed in database/SOP-GAPS.md.
7. Worst of the ten: a Query Sheet item raised in Phase C or D is thrown away by the database, no error shown, dialog closes as if it saved. Staff would have no idea.
8. "Send Invite" on the Staff page sends no email. It writes a row. Whoever was invited has to be told separately or nothing happens.
9. "Mark Complete & Notify Client" notifies nobody — the checkbox records that someone did it elsewhere. `client_notified_at` is never written either, so there is no record at all.
10. A workflow cannot be sent backwards. A problem found at Report Sender can only be fixed outside the system or by deleting the workflow, which takes its notes and messages with it.
11. Confirmed with Angelo that late uploads crediting the wrong month is a bug, not intent. A January statement sent in March ticks off March. Documented as known-wrong for now, in both the client and staff procedures.
12. Added a reset button to the chat header. It clears the conversation and starts a new session, so the flow stops believing it has already raised a ticket.
13. The assistant writes Markdown, so button names arrived as `**Upload**`. The widget draws them bold now instead of showing the asterisks.

Left a `sops` and a `profiles` table behind in the local supabase_db_ftg-support container — a verification script of mine committed where it should have rolled back. That instance was empty, nothing of anyone's was touched, and the drop is in yesterday's notes if it wants clearing.

The n8n side still needs work before this is usable. The agent greets instead of answering, because the client's question is not reaching it — the AI Agent node is reading `chatInput` from the Aggregate output, which only holds SOP rows. Angelo has the fix. It also escalates too eagerly; the prompt now makes it troubleshoot three times first, and there is a new procedure with five real reasons an upload fails, taken from the upload screen's own conditions.
