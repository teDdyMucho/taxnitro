EOD: TaxNitro – deleting works again, and clients stop getting the same folder twice

1. Fixed deleting a file. Every delete was refused with "that file is no longer there" while the file was sitting right there.
2. The check for the file could never find it. It used FOUND after an EXECUTE, which Postgres does not set.
3. Moving a file was broken the same way and is fixed too.
4. Tested both against a real Postgres. Deleting and moving work, and a file that is genuinely gone is still refused.
5. Fixed the duplicate folders on Adan Sanchez. The standard folders ignored the older shared ones, so clients got a second copy of Balance Sheet.
6. Added a delete button to a client's folders. Rename was the only thing a folder card had.
7. Files in a deleted folder are not deleted. They move back out to the folder they were in.
8. Deleting one of the old shared folders takes it from every client, so the confirmation says so first.

Both database files are applied and everything is pushed. The duplicates already on Adan's account still have to be deleted by hand; only new ones are prevented. Belly should refresh the app to get the delete button.
